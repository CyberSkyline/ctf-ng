name: Deploy to Staging from Workspace

on:
  workflow_dispatch:
  push:

jobs:
  deploy:
    runs-on: self-hosted
    environment: Staging

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        echo "${{ secrets.STAGING_SSH_PRIVATE_KEY }}" > ~/.ssh/deploy-key
        chmod 600 ~/.ssh/deploy-key
        ssh-keyscan -H ${{ secrets.STAGING_HOST_1 }} >> ~/.ssh/known_hosts
        ssh-keyscan -H ${{ secrets.STAGING_HOST_2 }} >> ~/.ssh/known_hosts
        chmod 600 ~/.ssh/known_hosts

    - name: Deploy to ctfng-staging-1
      run: |
        ssh -i ~/.ssh/deploy-key ubuntu@${{ secrets.STAGING_HOST_1 }} '
          cd /mnt/app-dev && \
          COMPOSE_PROJECT_NAME=staging-ng ENV_FILE=.env.staging docker compose -f docker-compose.prod.yaml down && \
          COMPOSE_PROJECT_NAME=staging-ng ENV_FILE=.env.staging docker compose -f docker-compose.prod.yaml up -d --build
        '

    - name: Deploy to ctfng-staging-2  
      run: |
        ssh -i ~/.ssh/deploy-key ubuntu@${{ secrets.STAGING_HOST_2 }} '
          cd /mnt/app-dev && \
          COMPOSE_PROJECT_NAME=staging-ng ENV_FILE=.env.staging docker compose -f docker-compose.prod.yaml down && \
          COMPOSE_PROJECT_NAME=staging-ng ENV_FILE=.env.staging docker compose -f docker-compose.prod.yaml up -d --build
        '

    - name: Cleanup
      run: |
        rm -f ~/.ssh/deploy-key
        rm -f ~/.ssh/known_hosts